package genek530.standAlone.hujowecommonsy;


import java.util.Collection;
import java.util.List;

public class permissionCHECK {

    /*
    public static boolean permissioncheck(List<Node> roleList, String whatpermission){
        boolean flag = false;
        for(Node node : roleList){
            if(node.getKey().equals(whatpermission)) flag = true;
        }
        return flag;
    }
    public static boolean permissioncheck(Collection<Node> roleList, String whatpermission){
        boolean flag = false;
        for(Node node : roleList){
            if(node.getKey().equals(whatpermission)) flag = true;
        }
        return flag;
    }


     */

}
